# assets/

- `icon.png` — **placeholder**, a solid OSU Scarlet (`#BA0C2F`) 1024x1024 square. It exists only so
  `app.json`'s `icon` field resolves for native builds (`expo prebuild` / `eas build`). Replace with
  real branded icon art during a design/branding pass.
